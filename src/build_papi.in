### Configure PAPI.
MY_PREFIX_DIR=`pwd | sed -e "s#papi-.*\/src#build#g"`
MY_CFLAGS=PKG_CPICFLAGS
./configure \
  --prefix=${MY_PREFIX_DIR} \
  CFLAGS=${MY_CFLAGS}

### Make and install.
make
make install
